# Step 1
## when u change the top, bottom, left and right values, the whole box moved depending on the values that was given. When its just default it just sticks to the side of the screen but if you add relative the box moves but still keeping it's shape.
# Step 2
## If you scroll the page the footer does not change. It stays at the bottom of the screen. Fixed means that the css element wont change its position in the viewport
# Step 3
## It removes an element from normal document flow and positions itself depending on the nearest position ancestor
# Step 4
## The z-index positions the element either infront z-index=1 or behind an element z-index = number>1. when you change the z index values it can change which element can be seen first.

# CHALLENGE

# Reflection Questions
